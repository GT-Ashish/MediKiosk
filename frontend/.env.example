# MediKiosk Frontend Environment Variables
# Copy to .env.local for local overrides (gitignored by Vite automatically).

# Backend API base URL (empty = same-origin, uses Vite proxy in dev)
VITE_API_BASE_URL=
