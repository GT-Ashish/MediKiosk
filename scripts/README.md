# MediKiosk Scripts

Utility and setup scripts for development, deployment, and maintenance.

## Status

Scripts will be added as needed during development.

## Planned Scripts

- `setup-dev.sh` / `setup-dev.ps1` — Development environment setup
- `run-tests.sh` — Run all test suites
- `seed-db.sh` — Seed the database with development data
- `lint.sh` — Run linters across all codebases
