"""
MediKiosk Domain Layer.

Contains all domain schemas (Pydantic models) that define the core
MediKiosk data contracts. These schemas are used across the API,
service, and infrastructure layers.

These are NOT database models. They are pure Python/Pydantic types
that express the domain concepts of a MediKiosk patient session.
"""
